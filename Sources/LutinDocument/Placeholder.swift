// Real code arrives in subsequent tasks. This file exists so SwiftPM can compile
// the empty target after it is declared in Package.swift.
import Foundation
public enum LutinDocumentTarget { public static let placeholder = true }
