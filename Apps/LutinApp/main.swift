import SwiftUI

@main
struct LutinApp: App {
    var body: some Scene {
        WindowGroup("Lutin") {
            Text("Lutin.app — Phase 1 scaffold")
                .frame(minWidth: 800, minHeight: 600)
        }
    }
}
